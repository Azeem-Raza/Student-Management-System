package azeem.java.server;

/*import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import azeem.java.server.data.entity.data;

import azeem.java.server.data.repo.dataRepository;

@SpringBootTest
class StudentManagementApplicationTests {
	
	@Autowired
	dataRepository repository;

	@Test
	void contextLoads() {
	}
	@Test 
	void createTest() {
		data d = new data(1, "Azeem", "Raza");
		repository.save(d);
		
	}
	@Test
	void readTest() {
		data d = repository.findById(4).get();
		assertThat(d).isNotNull();
		assertThat(d.getFirstname()).isEqualTo("Awais");
	}
	@Test 
	void updateTest() {
		data d = repository.findById(4).get();
		d.setStudents_ID(2);
		d.setFirstname("Ahmed");
		d.setLastname("Anwar");
		repository.save(d);
		
		
	}

}*/
